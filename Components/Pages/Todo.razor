@page "/todo"
@rendermode InteractiveServer
@using ToDoApp.Model

@*TodoListe Tutorial: https://www.youtube.com/watch?v=nJzgPOQF3tU&t=106s*@

<PageTitle>ToDo-App</PageTitle>

<h1>My ToDo's</h1>
<h3>left over Tasks: (@toDos.Count(toDo => !toDo.IsDone))</h3>

@*ToDo Form*@
<div class="todo-form">
        <input type="text" placeholder="Whats your task..." @bind="newToDo" />
        <button type="button" @onclick="AddToDo">Add Task</button>

</div>

@if (!string.IsNullOrEmpty(errorMessage))
{
    <div class="error-message">@errorMessage</div>
}

@* To do List*@
@if (toDos.Any()) //überprüfung, ob überhaupt Aufagebn vorhanden sind. 
{
    <ul>
        @foreach (var toDo in toDos)
        {
            <li>
                <input type="checkbox" @bind="toDo.IsDone" />
                <input type="text" @bind="toDo.Title" />
                <button type="button" @onclick=@(() => DeleteToDo(toDo))>Delete</button>
            </li>
        }
    </ul>
}
else
{
    <p>No ToDo's available</p>
}


@code {
    // Create
    private IList<ToDoItem> toDos = new List<ToDoItem>();
    private string newToDo;
    private string errorMessage = string.Empty;
    
    private void AddToDo()
    {
        if (string.IsNullOrEmpty(newToDo)) // Wenn -task leer erstellt wird
        {
            errorMessage = "Please enter a task.";
            return;
        } 

        // Wenn Task gültig, hinzufügen 
            toDos.Add(new ToDoItem()
            {
               Title=newToDo 
            });

        newToDo = string.Empty;
        errorMessage = string.Empty; // Fehlernachricht zurücksetzen
            
        }
    

    private void DeleteToDo(ToDoItem toDo)
    {
        toDos.Remove(toDo);
    }

}
